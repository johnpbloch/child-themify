const rest_url = window.ChildThemify.rest || '';
const rest_nonce = window.ChildThemify.nonce || '';
const theme_list = window.ChildThemify.themes || [];

const settings = {rest_url, rest_nonce, theme_list};

export {settings};
